__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.egg-info/
dist/
build/
.mypy_cache/

# Per-machine data directory (encrypted DB, salt, key_check, config, backups).
# Each machine creates its own passphrase and encrypted database on first run.
data/
